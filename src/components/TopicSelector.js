import React from 'react';

const TopicSelector = () => {
    return (
        <div></div>
    //    <select defaultValue={match.url} onChange={(e) => history.push(`${e.target.value}`)}>
    //        <option value={`${match.url}`}>All</option>
    //        <option value={`${match.url}/javascript`}>Javascript</option>
    //    </select>
    )
}

export default TopicSelector